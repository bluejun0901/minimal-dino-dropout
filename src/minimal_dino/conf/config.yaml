defaults:
  - data: default
  - model: bert_base
  - augmentation: dropout
  - objective: byol
  - optimization: default
  - teacher: default
  - evaluation: stsb
  - checkpoint: default
  - runtime: default
  - logging: default
  - _self_

hydra:
  run:
    dir: ${runtime.output_dir}
  job:
    chdir: false
  output_subdir: .hydra
